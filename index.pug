---
layout: layouts/base
---
h1 11d.im

h2 notices
ul
  each note in collections.notices
    li
      a(href=note.url) !{note.data.title}

h2 journal
ul
  each entry in collections.journal
    li
      span !{entry.date} 
      a(href=entry.url) !{entry.data.title}
